default: &default
  adapter: postgresql
  encoding: unicode
  database: mlb_dev
  pool: 5
  username: dan
  password:

development:
  <<: *default

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: mlb_test

production:
  <<: *default
  database: raceto13
  username: <%= ENV['MLB_DB_USER'] %>
  password: <%= ENV['MLB_DB_PASSWORD'] %>